
// Định nghĩa hàm myTrans
export const myTrans = (text) => {
    // Ở đây bạn có thể sử dụng logic dịch thuật, ví dụ: lấy text từ một file JSON hoặc từ API.
    // Hiện tại chỉ trả về chuỗi text gốc.
    return text;
};
